import {adminEmail,adminPassword,makeToken,json,readJson} from './_session.js';
export default async function handler(req,res){if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});try{const{email,password}=await readJson(req);if(String(email).toLowerCase()!==adminEmail.toLowerCase()||String(password)!==adminPassword)return json(res,401,{error:'Invalid administrator credentials'});return json(res,200,{ok:true,user:{id:'u1',name:'Library Admin',email:adminEmail,role:'admin'}},{'Set-Cookie':`sggs_admin_session=${encodeURIComponent(makeToken())}; Max-Age=28800; Path=/; HttpOnly; SameSite=Lax; Secure`})}catch{return json(res,400,{error:'Invalid request'})}}
