import React,{useMemo,useState} from 'react';
import {Link,useParams,useSearchParams} from 'react-router-dom';
import {ArrowRight,ArrowUpRight,BookOpen,Building2,FileText,GraduationCap,LibraryBig,MapPin,PlayCircle,Search,ShieldCheck,Star,BookMarked,Download,ExternalLink,ChevronRight} from 'lucide-react';
import {useLibrary} from '../../context/LibraryContext';
import {PageHero,SectionTitle,QuickCard,BookCard,Empty,ResourceCard} from '../../components/site';
export default function Catalogue(){const {books}=useLibrary();const [params]=useSearchParams();const [q,setQ]=useState(params.get('q')||'');const filtered=useMemo(()=>books.filter(b=>`${b.title} ${b.author} ${b.department}`.toLowerCase().includes(q.toLowerCase())),[books,q]);return <><PageHero eyebrow="CATALOGUE" title="Find your next book" text="Search the Shri Guru Gobind Singhji Institute of Engineering & Technology Central Library catalogue by title, author or department." image="/images/bookshelves.jpg"/><section className="container section"><div className="filter-bar"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search catalogue..."/><span>{filtered.length} records</span></div>{filtered.length?<div className="book-grid">{filtered.map(b=><BookCard book={b} key={b.id}/>)}</div>:<Empty title="No catalogue records found" text="Try a different title, author or department."/>}</section></>}
