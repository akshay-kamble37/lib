import React,{useMemo,useState} from 'react';
import {Link,useParams,useSearchParams} from 'react-router-dom';
import {ArrowRight,ArrowUpRight,BookOpen,Building2,FileText,GraduationCap,LibraryBig,MapPin,PlayCircle,Search,ShieldCheck,Star,BookMarked,Download,ExternalLink,ChevronRight} from 'lucide-react';
import {useLibrary} from '../../context/LibraryContext';
import {PageHero,SectionTitle,QuickCard,BookCard,Empty,ResourceCard} from '../../components/site';
export default function BookDetails(){const {id}=useParams();const {books}=useLibrary();const b=books.find(x=>x.id===id);if(!b)return <section className="container section"><Empty title="Book not found"/></section>;return <><PageHero eyebrow="CATALOGUE / BOOK" title={b.title} text={b.description}/><section className="container section"><div className="detail-card"><div className="large-cover"><BookOpen size={54}/><span>CENTRAL<br/>LIBRARY</span></div><div><span className="chip">{b.department}</span><h2>{b.title}</h2><p className="lead">by {b.author}</p><div className="detail-grid"><div><small>Publication year</small><b>{b.year}</b></div><div><small>ISBN</small><b>{b.isbn}</b></div><div><small>Availability</small><b>{b.available} of {b.copies} available</b></div><div><small>Location</small><b>{b.rack} / Shelf {b.shelf}</b></div></div><Link className="primary-btn" to="/catalogue">Back to catalogue</Link></div></div></section></>}
