import React,{useState} from 'react';
import {Link,Outlet,useLocation,useNavigate} from 'react-router-dom';
import {LayoutDashboard,BookOpen,Database,FileText,Megaphone,BookMarked,Building2,LogOut,Plus,Trash2,RotateCcw,Home,Upload,Save,Image,Settings2} from 'lucide-react';
import {useLibrary} from '../../context/LibraryContext';
import {ManagerForm,DataTable,Field,AdminPage,Stat,Step} from '../../components/admin';
export default function ManageBooks(){const {books,setBooks,departments,setToast}=useLibrary();const [form,setForm]=useState({title:'',author:'',department:departments[0].name});const add=e=>{e.preventDefault();if(!form.title.trim())return;setBooks(v=>[{id:'b'+Date.now(),...form,year:new Date().getFullYear(),isbn:'',copies:1,available:1,rack:'NEW',shelf:'01',description:'Catalogue record added by library administrator.'},...v]);setForm({title:'',author:'',department:departments[0].name});setToast('Catalogue record added')};return <AdminPage eyebrow="CATALOGUE" title="Manage books"><ManagerForm title="Add catalogue record" onSubmit={add}><Field label="Title"><input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></Field><Field label="Author"><input value={form.author} onChange={e=>setForm({...form,author:e.target.value})}/></Field><Field label="Department"><select value={form.department} onChange={e=>setForm({...form,department:e.target.value})}>{departments.map(d=><option key={d.name}>{d.name}</option>)}</select></Field><button className="primary-btn full"><Plus/> Add book</button></ManagerForm><DataTable headers={['Book','Department','Availability','Action']} rows={books.map(b=><><td><b>{b.title}</b><small>{b.author}</small></td><td>{b.department}</td><td>{b.available}/{b.copies}</td><td><button className="danger" onClick={()=>{setBooks(v=>v.filter(x=>x.id!==b.id));setToast('Book removed')}}><Trash2 size={14}/></button></td></>)}/></AdminPage>}
