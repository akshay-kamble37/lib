import React,{useState} from 'react';
import {Link,Outlet,useLocation,useNavigate} from 'react-router-dom';
import {LayoutDashboard,BookOpen,Database,FileText,Megaphone,BookMarked,Building2,LogOut,Plus,Trash2,RotateCcw,Home,Upload,Save,Image,Settings2} from 'lucide-react';
import {useLibrary} from '../../context/LibraryContext';
import {ManagerForm,DataTable,Field,AdminPage,Stat,Step} from '../../components/admin';
export default function ManageResources(){const {resources,setResources,setToast}=useLibrary();const [form,setForm]=useState({name:'',url:'',category:'Academic Database'});const add=e=>{e.preventDefault();if(!form.name.trim())return;setResources(v=>[{id:'r'+Date.now(),...form,description:'Academic resource maintained by SGGS Central Library.'},...v]);setForm({name:'',url:'',category:'Academic Database'});setToast('Resource added')};return <AdminPage eyebrow="E-RESOURCES" title="Manage digital resources"><ManagerForm title="Add resource" onSubmit={add}><Field label="Name"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field><Field label="URL"><input value={form.url} onChange={e=>setForm({...form,url:e.target.value})}/></Field><Field label="Category"><select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option>Academic Database</option><option>Engineering Database</option><option>Discovery</option></select></Field><button className="primary-btn full"><Plus/> Add resource</button></ManagerForm><DataTable headers={['Name','Category','URL','Action']} rows={resources.map(r=><><td><b>{r.name}</b><small>{r.description}</small></td><td>{r.category}</td><td>{r.url}</td><td><button className="danger" onClick={()=>{setResources(v=>v.filter(x=>x.id!==r.id));setToast('Resource removed')}}><Trash2 size={14}/></button></td></>)}/></AdminPage>}
